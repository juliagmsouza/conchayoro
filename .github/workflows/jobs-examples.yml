name: jobs examples

on:
  push
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: exemple of invalid command
        run: ecko Hello, world! build failed.
        
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: step do deploy
        run: echo Hello, world! deploy.
